spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/ticket-service
    username: root
    password: admin

  banner:
    location: banner.txt

ticket-service:
  screening:
    break-length: 10
  price:
    base: 1500
  date-time:
    pattern: yyyy-MM-dd HH:mm
